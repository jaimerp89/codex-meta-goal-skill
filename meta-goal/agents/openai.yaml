interface:
  display_name: "Meta Goal"
  short_description: "Compile objectives into /goal commands."
  default_prompt: "Use $meta-goal to compile this objective into a Codex CLI /goal command: "
policy:
  allow_implicit_invocation: false
